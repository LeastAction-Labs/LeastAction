name: demo_project
version: '1.0.0'
config-version: 2

profile: demo

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

on-run-start:
  - "{{ create_cube_functions() }}"
